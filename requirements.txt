# Faster-Whisper for GPU-accelerated speech recognition
faster-whisper==1.0.3

# PyTorch with CUDA 11.8 support (for GPU acceleration on RTX 3050 Ti)
# Install with: pip install -r requirements.txt
torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0
